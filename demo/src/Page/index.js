import React from 'react';
import store from '../store/index'

function Index(){
    return(<div>
        {/* id: {s.value.user_id}<hr />
        password: {s.value.user_pwd}<hr /> */}
    </div>)
}
export default Index;
